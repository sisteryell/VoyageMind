Synthesize the following agent recommendations for {{ country }} and select the TOP {{ city_count }} cities that best fit this specific traveler.

Traveler constraints (HARD REQUIREMENTS — do not ignore these):
- Budget: {{ budget }}
- Trip length: {{ duration }} days
- Travel styles: {{ travel_styles | join(', ') if travel_styles else 'general tourism' }}

{% for agent in agent_results %}
**{{ agent.agent_name | title }} Agent Recommendations:**
{% for rec in agent.recommendations %}
- {{ rec.city }} (score: {{ rec.confidence_score }}): {{ rec.reason }}
{% endfor %}

{% endfor %}
Selection rules:
1. FIRST: Eliminate any city that is clearly unsuitable for a {{ budget }} budget traveler on a {{ duration }}-day trip — even if multiple agents recommended it.
2. PREFER cities that appear in 2 or more agent lists, but only if they are a genuine fit for the budget and duration.
3. MATCH to travel styles: {% if travel_styles %}A traveler interested in {{ travel_styles | join(', ') }} should get different cities than a generic tourist.{% else %}Apply balanced considerations.{% endif %}
4. DIVERSIFY: The chosen cities should offer meaningfully different experiences where possible.
5. EXPLAIN specifically: Each reason must state why this city fits the {{ budget }} budget, the {{ duration }}-day trip, and the travel styles.

Do NOT default to the most famous tourist cities in {{ country }} if they are a poor fit for this traveler's constraints.

Final output checklist before you answer:
- Return exactly {{ city_count }} UNIQUE cities in valid JSON array format
- Each reason must explicitly mention budget fit, duration fit, and travel-style fit
- Prioritize multi-agent agreement only when it still satisfies traveler constraints
- Keep reasons concise and specific (1-3 sentences)
