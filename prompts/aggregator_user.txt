Synthesize the following agent recommendations for {{ country }} and select the TOP 2 cities that best fit this specific traveler.

Traveler constraints (HARD REQUIREMENTS — do not ignore these):
- Budget: {{ budget }}
- Trip length: {{ duration }} days
- Travel styles: {{ travel_styles | join(', ') if travel_styles else 'general tourism' }}

**History & Culture Agent Recommendations:**
{% for rec in history_recommendations %}
- {{ rec.city }} (score: {{ rec.confidence_score }}): {{ rec.reason }}
{% endfor %}

**Food & Cuisine Agent Recommendations:**
{% for rec in food_recommendations %}
- {{ rec.city }} (score: {{ rec.confidence_score }}): {{ rec.reason }}
{% endfor %}

**Transportation & Connectivity Agent Recommendations:**
{% for rec in transportation_recommendations %}
- {{ rec.city }} (score: {{ rec.confidence_score }}): {{ rec.reason }}
{% endfor %}

Selection rules:
1. FIRST: Eliminate any city that is clearly unsuitable for a {{ budget }} budget traveler on a {{ duration }}-day trip — even if all three agents recommended it.
2. PREFER cities that appear in 2 or more agent lists, but only if they are a genuine fit for the budget and duration.
3. MATCH to travel styles: {% if travel_styles %}A traveler interested in {{ travel_styles | join(', ') }} should get different cities than a generic tourist.{% else %}Apply balanced considerations.{% endif %}
4. DIVERSIFY: The two chosen cities should offer meaningfully different experiences where possible.
5. EXPLAIN specifically: Each reason must state why this city fits the {{ budget }} budget, the {{ duration }}-day trip, and the travel styles.

Do NOT default to the two most famous tourist cities in {{ country }} if they are a poor fit for this traveler's constraints.
